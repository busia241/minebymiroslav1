# Настройки проекта в Vercel Dashboard

## ⚙️ Настройки для успешного деплоя:

Зайдите в **Project Settings** → **Build & Development Settings** и установите:

### Framework Preset
- Выберите: **`Other`** или **`None`**

### Build Command
- Оставьте **пустым** или укажите: `echo 'No build'`

### Output Directory
- Оставьте **пустым** или укажите: `.` (точка)

### Install Command
- Оставьте **пустым** или: `npm install`

### Root Directory
- Укажите: `./` (точка слэш)

### Development Command
- Оставьте **пустым**

---

## 🔄 После изменения настроек:

1. Сохраните настройки
2. Перейдите в **Deployments**
3. Нажмите на последний failed deployment
4. Нажмите **"Redeploy"**

Или просто запушьте изменения в GitHub - Vercel автоматически пересоберет проект.

---

## ✅ Альтернатива: Создать папку public

Если Vercel все еще требует папку `public`, выполните:

```bash
# Создать папку public
mkdir public

# Переместить статические файлы
mv index.html console.html exit.html public/

# НО: папку api/ оставить в корне!
# api/ должна остаться в корне проекта
```

Затем в `vercel.json` измените:
```json
{
  "outputDirectory": "public",
  "rewrites": [...]
}
```

Но это изменит структуру проекта. Лучше сначала попробовать с настройками выше.

