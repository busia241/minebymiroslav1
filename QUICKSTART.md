# Быстрый старт - Minedrop Game by miroslav

## 🚀 За 5 минут до запуска

### 1. Создание иконок расширения (1 минута)

1. Откройте файл `extension/create-icons.html` в браузере
2. Нажмите кнопку "Создать все иконки"
3. Файлы `icon16.png`, `icon48.png`, `icon128.png` автоматически скачаются
4. Переместите эти 3 файла в папку `extension/`

### 2. Установка расширения (1 минута)

1. Откройте Chrome: `chrome://extensions/`
2. Включите "Режим разработчика" (справа вверху)
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку `extension`
5. ✅ Готово! Расширение установлено

### 3. Деплой на Vercel (3 минуты)

#### Вариант A: Через GitHub (рекомендуется)

```bash
# 1. Создайте репозиторий на GitHub

# 2. В этой папке выполните:
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ВАШ-USERNAME/ВАШ-РЕПОЗИТОРИЙ.git
git branch -M main
git push -u origin main

# 3. На vercel.com:
# - Войдите через GitHub
# - Add New Project → выберите репозиторий
# - Deploy
```

#### Вариант B: Прямой деплой через Vercel CLI

```bash
# Установите Vercel CLI (если еще нет)
npm i -g vercel

# В папке проекта:
vercel

# Следуйте инструкциям
```

### 4. Проверка работы

1. Откройте ваш сайт на Vercel
2. Перейдите на `/console.html` или `/minedrop/console`
3. Откройте консоль браузера (F12)
4. Должно быть видно: `[Minedrop Extension by miroslav] Content script loaded`
5. Игра должна загрузиться с бонусами!

## 📝 Что дальше?

- Измените бонусы в `extension/content.js` (строки с `+ 1000` и `* 1.5`)
- Настройте свой домен в Vercel
- Добавьте аутентификацию (если нужно)

## ❓ Проблемы?

- **Расширение не работает?** Проверьте консоль браузера (F12)
- **Иконки не создаются?** Создайте вручную: 16x16, 48x48, 128x128 PNG с буквой "M"
- **Vercel не деплоит?** Проверьте `vercel.json` и структуру папок

## 🎯 Готово!

Теперь у вас есть:
- ✅ Веб-сайт с игрой Minedrop
- ✅ Расширение для браузера с бонусами
- ✅ Все настроено для деплоя на Vercel

**Автор: miroslav**

